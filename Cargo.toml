[package]
name = "feature-flagging-service"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bson = "2.0.0"
mongodb = "2.0.0"
serde   = { version = "1.0", features = ["derive"] }
tokio   = { version = "1", features = ["full"] }
